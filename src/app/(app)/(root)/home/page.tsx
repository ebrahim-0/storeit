import Icon from "@/components/Icon";

const page = () => {
  return (
    <div>
      <Icon
        id="Illustration"
        width={342}
        height={342}
        className="transition-all hover:rotate-2 hover:scale-105"
      />
    </div>
  );
};

export default page;
